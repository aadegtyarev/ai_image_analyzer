# ai_image_analyzer
Python-инструмент для пакетного анализа изображений с использованием OpenAI API и совместимых провайдеров (локальные модели, прокси-серверы, OpenAI-совместимые сервисы).

---

## ✔ requirements.txt

```
openai>=1.54.0
Pillow>=11.0.0
python-dotenv>=1.0.1
```

---

## ✔ Пример `.env`

Сохраните как `.env` рядом со скриптом:

```
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT=60
OPENAI_MAX_TOKENS=1024

IMAGE_MAX_SIZE=1024
IMAGE_QUALITY=85

COLLAGE_MAX_SIZE=2048
COLLAGE_QUALITY=85

PROMPT_FILE=prompt.txt
```

`PROMPT_FILE` → текстовый файл с системным промптом.

---

## ✔ README.md

````
# ai_image_analyzer

Скрипт для анализа изображений и коллажей через OpenAI-совместимую Vision-модель.
Поддерживает:
- одну картинку → один запрос
- несколько картинок (до 10) → ресайз → коллаж → один запрос
- режим override-текста (`-t`)
- чисто текстовые запросы без изображений
- конфигурацию через .env
- работу из CLI и лёгкую интеграцию в бота

---

## ⚙ Установка

Требуется Python 3.9+

```bash
git clone <ваш_репозиторий>
cd <папка_проекта>
````

Установите зависимости:

```bash
pip install -r requirements.txt
```

---

## 🧩 Настройка .env

Создайте `.env` рядом со скриптом:

```bash
cp .env.example .env
```

Отредактируйте нужные значения:

* ключ OpenAI
* модель
* параметры изображений
* PROMPT_FILE

---

## ▶ Запуск

### 1️⃣ Одна картинка

Создает `image_analyse.txt`

```bash
python ai_image_analyzer.py photo.jpg
```

---
```bash
-q скрывает лишний вывод.
```

### 2️⃣ Несколько картинок (supports `*`)

До 10 файлов, создаётся коллаж и `group_YYYYMMDD_HHMMSS_analyse.txt`

```bash
python ai_image_analyzer.py "images/*.jpg"
```

---

### 3️⃣ Картинка + текст (PROMPT_FILE игнорируется)

Ответ выводится в консоль, файл не создаётся

```bash
python ai_image_analyzer.py photo.jpg -t "Опиши композицию"
```

---

### 4️⃣ Только текст

Без изображений, только ответ модели

```bash
python ai_image_analyzer.py -t "Проанализируй идею фотосерии"
```

---

## 📦 Результат

* Для одной картинки:
  `photo_analyse.txt`
* Для группы:
  `group_20251222_204455_analyse.txt`
* В stdout при использовании `-t`

---

## 🧠 Замечания

* максимум 10 изображений на запрос
* изображения автоматически сжимаются
* коллаж строится автоматически
* OpenAI session создаётся на каждый запрос
* легко выносится в Telegram-бот
