# ai_image_analyzer

Python‑инструмент для анализа фотографий с помощью OpenAI‑совместимых vision‑моделей.

Кратко:
- одна картинка → один запрос, результат сохраняется рядом с файлом
- несколько картинок (до 10) → ресайз → коллаж → один запрос
- режим `--per-image` — отдельный запрос и файл на каждую картинку
- текстовая надстройка (`-t`) и чисто текстовые запросы
- конфигурация через `.env` с переопределением параметров через CLI
- может работать как CLI и как модуль (JSON‑API/бот)

---

## ⚙ Установка

Требуется **Python 3.10+**.

Клонируем репозиторий и устанавливаем зависимости:

```bash
git clone <ваш_репозиторий>
cd <папка_проекта>
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Содержимое `requirements.txt` (основное):

```text
openai>=1.54.0
Pillow>=11.0.0
python-dotenv>=1.0.1
aiogram>=3.0.0
requests>=2.0.0
```

---

## 🧩 Настройка `.env`

Создайте `.env` рядом с `bot.py` (или используйте существующий). Простой способ:

```bash
cp .env.example .env
Пример переменных (убедитесь, что задали `BOT_TOKEN` и `BOT_ADMIN_ID`):
BOT_TOKEN=your_tg_bot_token
BOT_ADMIN_ID=123456789

OPENAI_API_KEY=your_openai_key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

### Environment variables

You can control admin notifications for edit failures using `NOTIFY_ON_EDIT_FAILURE` (default: disabled).

- `NOTIFY_ON_EDIT_FAILURE=1` — send admin a short notification when the bot fails to edit a kickoff message and falls back to sending a new message.
- `NOTIFY_ON_EDIT_FAILURE=0` — (default) do not send admin notifications on edit failures.
IMAGE_MAX_SIZE=1024
IMAGE_QUALITY=85
PROMPT_FILE=prompts/art_analysis.txt
```

* `OPENAI_BASE_URL` — можно указывать прокси/совместимые сервисы.
* Все параметры `.env` можно **переопределить из CLI** одноимёнными флагами:
  * `--OPENAI_MODEL`, `--OPENAI_TIMEOUT`, `--IMAGE_MAX_SIZE` и т.д.

* `OPENAI_MAX_TOKENS` отвечает за максимальное число токенов, которое модель может сгенерировать (параметр `max_tokens`).
* `OPENAI_BALANCE_THRESHOLD` — опциональный числовой порог баланса (в валюте сервиса). Если включена проверка баланса (`--check-balance`) и фактический баланс меньше порога, скрипт выведет предупреждение.
* Поля `usage` (prompt_tokens/completion_tokens/total_tokens/total_cost) возвращаются при доступности от провайдера. `total_cost` округляется до 3 знаков после запятой. В CLI выводится `USAGE:` с этой информацией после каждого анализа (если не указан `-q`).

---

## 🖼 CLI-использование

Общий формат:

```bash
python ai_image_analyzer.py [OPTIONS] [images...]

* `-p, --PROMPT_FILE` — переопределить файл промта (`PROMPT_FILE` из `.env`).
* `-t, --text` — текст для отправки как `user` (PROMPT_FILE игнорируется, файл не создаётся).
* `--check-balance` — проверить баланс провайдера и вывести JSON.
* `-q, --quiet` — тихий режим (минимум логов, подавляет вывод USAGE).

Параметры окружения, которые можно задать через CLI:

```text
--OPENAI_API_KEY, -k
--OPENAI_BASE_URL, -u
--OPENAI_MODEL, -m
--OPENAI_TIMEOUT, -T
--OPENAI_MAX_TOKENS, -M
--IMAGE_MAX_SIZE, -s
--IMAGE_QUALITY, -Q
--COLLAGE_MAX_SIZE, -S
--COLLAGE_QUALITY, -G
--PROMPT_FILE, -p
```

### 1️⃣ Одна картинка

Создаёт `photo_analyse.md` рядом с файлом:

```bash
python ai_image_analyzer.py images/photo.jpg
```

### 2️⃣ Несколько картинок (до 10, коллаж)

Создаётся коллаж и один общий файл `group_YYYYMMDD_HHMMSS_analyse.txt`:

```bash
python ai_image_analyzer.py "images/*.jpg"
```

### 3️⃣ Режим `--per-image` (отдельный файл на каждую)

Каждая картинка → отдельный запрос → `*_analyse.md`:

```bash
python ai_image_analyzer.py --per-image "images/*.jpg"
```

### 4️⃣ Картинка + текст (PROMPT_FILE игнорируется)

Ответ выводится в консоль, файлы не создаются:

```bash
python ai_image_analyzer.py images/photo.jpg -t "Разбери кадр как наставник"
```

### 5️⃣ Только текст

Без изображений, чистый текстовый запрос:

```bash
python ai_image_analyzer.py -t "Предложи идею фотосерии о зимней меланхолии"
```

---

## 📂 Промпты

По умолчанию промпты лежат в каталоге `prompts/`:

* `PROMPT_FILE` указывает путь к файлу с системным промтом.

Пример структуры:

```text
prompts/
  art_analysis.txt
  series_idea.txt
  mentor_simple.txt
```

---

## 📝 Замечания

* Максимум **10 изображений** в одном запросе.
* Все изображения автоматически ресайзятся до `IMAGE_MAX_SIZE` по длинной стороне.
* Коллаж автоматически подбирает сетку (N×N, N×M).
* В CLI для каждого запроса создаётся своя OpenAI-сессия.

📌 См. `CHANGELOG.md` для списка последних исправлений и изменений.

## 🐞 Отладка

Для локальной диагностики используйте:

- `DEBUG=1` — общий отладочный вывод (короткие snippet'ы system/user prompt и служебные сообщения).
- `IMAGE_DEBUG=1` — печатает информацию по изображениям (`[IMAGE_DEBUG] image #...`).

- `DEBUG=1` — общий отладочный вывод (короткие snippet'ы system/user prompt и служебные сообщения). При включении `DEBUG` также печатаются сообщения о кэшировании промтов для альбомов (`[MEDIA_DEBUG] set media context ...` / `[MEDIA_DEBUG] expired media context ...`).
- `IMAGE_DEBUG=1` — печатает информацию по изображениям (`[IMAGE_DEBUG] image #...`).

Пример:

```bash
DEBUG=1 IMAGE_DEBUG=1 python bot.py
```

Docker (basic):

```bash
docker compose up --build -d
```

---

## Deploy (install-as-service)

Рекомендуемый путь на сервере: `~/applications/ai_image_analyzer/`.

Запуск автоматизированного скрипта (под `sudo`):

```bash
sudo ./deploy/install-as-service.sh \
  --install-dir /home/$USER/applications/ai_image_analyzer \
  --user $USER
```

Коротко, что делает скрипт:
- клонирует/обновляет репозиторий в указанной папке;
- создаёт `venv` и устанавливает зависимости;
- создаёт `.env` (если его нет) и сообщает, что нужно поместить туда `BOT_TOKEN` и `BOT_ADMIN_ID`;
- ставит systemd unit `/etc/systemd/system/ai_image_analyzer.service`, включает и запускает сервис;
- логи доступны: `sudo journalctl -u ai_image_analyzer -f`.

Если вы хотите запускать бота под своим аккаунтом (user unit) или вручную — см. `deploy/README.md`.

---

### Удаление неиспользуемого модуля `prompts_utils.py`

В репозитории был файл `prompts_utils.py`, но его функция `load_prompts` была намеренно удалена (она бросает RuntimeError) и не используется в кодовой базе. Я удалил этот файл из репозитория — используйте `bot.load_prompts` и текущую реализацию промптов в `bot.py`.


```
::contentReference[oaicite:1]{index=1}
```
