Examples
========

This folder contains small runnable examples demonstrating how to use the
`ai_image_analyzer` package programmatically.

- `analyzer_service_example.py` — shows creating a simple `AnalyzerService`
  with a dummy provider and making text/image calls.

Run an example:

```bash
python examples/analyzer_service_example.py
```
